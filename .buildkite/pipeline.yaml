steps:
  - label: ":h200: gsm8k qwen3.5"
    agents:
      queue: H200
    timeout_in_minutes: 120
    commands:
      - python3 -m ensurepip --upgrade --default-pip 2>/dev/null || curl -fsSL https://bootstrap.pypa.io/get-pip.py | python3 - --user
      - python3 -m pip install --user --upgrade lm-eval pyyaml
      - export PATH="$HOME/.local/bin:$PATH" && ./run.sh workloads/qwen3_5_h200.yaml
    artifact_paths:
      - "results/**/*"
